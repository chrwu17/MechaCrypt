## EquInvCipher()

The code in this directory implements the Equivalent Inverse Cipher algorithm, for decrypting data following psedocode and algorithms outlined in Section 5.3 of AES Standard: NIST FIPS 197